<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="/socket.io/socket.io.js"></script>
    <script src="../public/javascripts/chat.js"></script>
</head>
<body onload="init('{{roomName}}')">
{{> header }}

<div class="container mt-5">
    <h2>Welcome to the Chat Room</h2>
    <p><strong>Topic:</strong> {{topic}} {{#if movieTitle}}- Movie: {{movieTitle}}{{/if}}</p>

    <!-- Form di ingresso -->
    <div id="initial_form" style="display: block;">
        <h3>Join the Room</h3>
        <form onsubmit="return false;">
            <div class="mb-3">
                <label for="name" class="form-label">Your Name:</label>
                <input type="text" id="name" class="form-control" required>
            </div>
            <div class="mb-3">
                <label for="roomNo" class="form-label">Room ID:</label>
                <input type="text" id="roomNo" class="form-control" value="{{roomName}}" readonly>
            </div>
            <button class="btn btn-primary" onclick="connectToRoom()">Connect</button>
        </form>
    </div>

    <!-- Interfaccia chat -->
    <div id="chat_interface" style="display: none;">
        <div class="mb-3">
            <p><strong>{{whoYouAre}}</strong>, you are in room: <span id="in_room"></span></p>
        </div>
        <div id="history" class="border p-3 mb-3" style="height: 300px; overflow-y: scroll; background-color: #f8f9fa;"></div>
        <div class="input-group">
            <input id="chat_input" type="text" class="form-control" placeholder="Type a message..." autocomplete="off">
            <button class="btn btn-success" onclick="sendChatText()">Send</button>
        </div>
    </div>
</div>
</body>
</html>